import react from @react 
import controlls  form './pathfinder.json'
import droneivi
  

consit block1
consit block2

funtion User(){
<div>
<form>
<lable> user name </lable>
<input type="text">
<br>
<lable>enter your  Password</lable>
<input type="password">
</form>
</di>
}
funtion make Order()
{
return (

const region
 if (region ===region){
   <p>please enter your location</p>
<input tpye="text" className="Loction"></input>
 }

)
}
funtion  droneivi()
{
user()
return (
controlls .map((return (controlls.code)=>
<div> block1=controlls.name
e</div>
<video id="controll.cam1" autoplay="" src="" style="background: none;">controll.cam1000000</video>
<di>
<button> view Order </button>
<button> drop order</button>
<button> pick up order<button>
</div>
)

}

expect defult droneivi()

