
version: '3.4'

services:
  astaralgorithm:
    image: astaralgorithm
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        - configuration=Debug
    volumes:
      - ~/.vsdbg:/remote_debugger:rw
